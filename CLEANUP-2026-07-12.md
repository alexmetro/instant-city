# CLEANUP INVENTORY — 2026-07-12 (post-Great-Split room cleaning)

## RESOLUTION — execution sprint, 2026-07-12 (same day)

Executed against this inventory as the work order. Acceptance run per layers-spec §Assembly:
rebuild via tools/build-app.js (62 → 74 chunks — every relocation is a position-preserving
chunk split, so moved code keeps its exact global statement order, rngBuild() stream and
scene.add/listener order included); pixel-compare at the canonical six framings vs the
pre-cleanup build (headless Chrome, same capture harness as the Great Split verification):
cross-build diff BELOW the same-build repeat-capture noise floor at all six (f1 0.034%
vs floor 0.050% · f2 0.035%/0.053% · f3 0.20%/0.49% · f4 1.91%/6.95% · f5 0.76%/2.71% ·
f6 0.008%/0.024%); `__P1850.audits.runAll()` 11/11 green, every result byte-identical to
the pre-cleanup run; all numeric QA hooks byte-identical (only wall-clock artifacts vary:
pelican flight phase, tile-paint ms — both vary identically between same-build runs);
console identical (37 lines, only the tile-kit timing number differs, as it does between
same-build runs). `watch`/`speedKey`/`flyTo`/speed-pill hooks re-verified responding after
the D10 de-dup and M9/M10 moves.

| Item | Status | Note |
|---|---|---|
| D1 graphPath | DONE | deleted (+S8 comment) |
| D2 TRADES quartet | DONE | deleted (+S7 comment; tombstone note left) |
| D3 worksiteForTrade | DONE | deleted |
| D4 loopPoints | DONE | deleted |
| D5 WORKSITE_CHURCH | DONE | deleted |
| D6 STREET_DIRT_COL/SURVEY_GHOST_COL | DONE | deleted incl. attached stale header |
| D7 shipPathPoint | DONE | deleted (+S3) |
| D8 _faunaM4 | DONE | deleted |
| D9 LIMB_STATIC | DONE | name dropped; "id 0 = static" note kept inline |
| D10 duplicate watch/speedKey keys | DONE | earlier (dead) members deleted; hooks verified live |
| D11 VARA_M→BLOCK_W/D chain | DONE | deleted; vara/block research figures preserved as a comment (+S9) |
| D12 CATALOG_ACT_BY_ID | DONE | deleted |
| D13 reactions catalog | DONE | per sprint directive: script tag + loader removed; catalog-reactions.js left on disk with a re-add note in 03-sim |
| M1 fauna home-assignment block | DONE | HOME_BUILDING_SPOTS/nextHome/homeLabelFor → people.js (chunk-50 top, same global position — rngBuild stream unchanged); dayForDensityAtLeast → core/03-sim beside DENSITY_CURVE |
| M2 west-peninsula fauna/flora in terrain | DEFERRED | RISKY per inventory (tagInspect/_westFauna must move intact; ~200 lines) |
| M3 geographic label DOM in terrain | DONE | LABELS.push + buildLabels/_geoLabelBandOpacity/updateLabels → labels-inspect (new chunk right after the terrain chunk; runs before first frame — analyzed safe) |
| M4 terrainMat/terrainMesh in ground-paint | DONE | → terrain.js, exact position (chunk split) |
| M5 route-label LABELS.push in ground-paint | DEFERRED | pushes read IIFE locals (midPt/farPt/pmid) — not verbatim-movable; needs a small route-data interface first |
| M6 makeOutpostPersonGeo/placeOutpostPerson | DONE | + _outpostSkin → people.js, exact position (buildings chunk-18 split) |
| M7 VENUE_KEY_RESOLVERS/worksiteForOccupation | DONE | → people.js end of homes-&-workplaces chunk (lazy closures; fauna's extensions still precede all consumers) |
| M8 resetFleeBudget/applyFireCrowd | DONE | → people.js (it moves people); the undocumented second effects↔people seam is gone — no spec edit needed |
| M9 speed-pill DOM + keydown in core | DONE | → camera-input, exact positions (03-sim chunk split ×2); setSimSpeed state stays core |
| M10 flyTo + pulse crosshair | DONE | → camera-input, exact position (rig write belongs to the rig owner); callers unchanged |
| M11 zones-tint cross-layer mesh writes | DEFERRED | needs a buildings setStructureFade(t) interface + spec edit; latent growthBuildingMesh2 fade bug flagged as its own fix task (behavior change — out of cleanup scope) |
| M12 NAMED_BUILDING_SPOTS | DONE | → buildings.js right after the chapel push (final named set identical — no pushes after) |
| M13 OUTPOSTS + LUMBER_YARD_SPOT | DONE | OUTPOSTS → core/01-geography (pure literal, earlier init safe); lumber yard → buildings.js, exact position (doodads chunk-37 split) |
| U1 point-to-segment ×5 | PARTIAL | doodads' local distToSeg deleted (2 call sites → core distToSegXZ); labels-inspect's _distToSegXZ SKIPPED — different contract (returns {d,px,pz} projection, not the scalar); squared hot-path variants untouched per inventory |
| U2 duplicate pick() | DONE | hoisted once to doodads top level (no top-level name collision — verified) |
| U3 Catmull-Rom ×2 | SKIPPED | optional per inventory |
| U4 escapeHTML/cap | DONE | → core/00-boot |
| U5 DOW_NAMES | DONE | → core/03-sim (calendar = core clock domain) |
| U6 _microHash/_doodadHash | SKIPPED | optional per inventory |
| U7 radial-gradient sprites | SKIPPED | optional per inventory |
| S1–S10 stale comments | DONE (10/10) | spring-rig ×2 rewritten to the Annals chase rig; "3 call sites" gone with D7; .ship-label/.building-label → current classes; CHUNK-12 tombstone rewritten (no more paintStub/STREET_MAIN_W ghosts); .geo-label → .wlbl-zone; TRADES/constable comment gone with D2; graphPath comment gone with D1; BLOCK_W claim gone with D11; 03-sim warn text now truthful |
| C1 .pp-nopaper-since | DONE | deleted |
| X1 reactions sidecar | DONE | see D13 |
| X2/X3 events `pos`, prices `kind`/`ctx` | DEFERRED | builder-side (tools/build-events.js, build-prices.js) — outside app src; data sidecars untouched by this sprint |

---

QA-GATE read-only audit of `app/src/core/*.js` + `app/src/layers/*.js` + `src/shell.html` (18,078 lines).
Method: mechanized reference-count over every top-level declaration (all src files + shell.html + index.html
counted as haystack, so string/registry references were not missed), followed by a full manual read of every
file for misplacement, duplication, and stale documentation. Nothing was edited except this report.

## Summary

| Category | Count | Est. lines involved |
|---|---|---|
| D. Dead code (zero call sites, verified) | 13 items | ~90 lines JS + 1 unused data sidecar |
| M. Misplaced code (wrong layer per layers-spec OWNS) | 13 items | ~450 lines implicated |
| U. Duplication | 7 clusters | ~60 redundant lines |
| S. Stale comments / docs | 10 items | ~40 comment lines that actively mislead |
| C. CSS orphans (shell.html) | 1 selector | ~4 lines |
| X. Unused data-sidecar fields | 3 findings | data-only (no src lines) |

Per-file tallies (dead/misplaced/stale): people.js 4/1/1 · buildings.js 1/2/0 · ground-paint.js 1/2/2 ·
ships.js 1/0/1 · fauna.js 1/1/0 · terrain.js 0/3/0 · core/05-routing.js 1/0/1 · core/03-sim.js 2/1/0 ·
core/06-debug.js 1/0/0 · core/01-geography.js 1/1/1 · core/07-main.js 0/0/1 · camera-input.js 0/0/1 ·
director.js 0/0/1 · ui-chrome.js 0/1/0 · zones-tint.js 0/1/1 · doodads.js 0/1/0 · labels-inspect.js 0/1/0 ·
effects.js 0/1/0 · shell.html 0/0/0 (+1 CSS orphan).

### Top 10 safest big wins (delete/fix with zero behavior risk)
1. **D1** `graphPath()` — 23-line dead Dijkstra in core/05-routing.js.
2. **D3** `worksiteForTrade()` — 19-line dead legacy trade switch in people.js.
3. **D2** the `TRADES` / `TRADES_WITH_CONSTABLE` / `SFPD_FOUNDING_DAY` / `TRADE_EARLIEST_DAY` quartet (people.js, ~12 lines incl. its misleading comment).
4. **D11** `VARA_M`→`LOT_M`→`BLOCK_W`/`BLOCK_D` dead constant chain (core/01-geography.js, ~10 lines) + its stale "coarse consumers read this" claim.
5. **S1+S2** the two "existing spring rig" comments (core/07-main.js:52, director.js:13) — the single most misleading survivors of the killed spring-damper camera.
6. **D6** `STREET_DIRT_COL`/`SURVEY_GHOST_COL` + attached stale header (ground-paint.js:940-944, 5 lines).
7. **D4** `loopPoints()` (people.js:234, 5 lines).
8. **D10** duplicate `watch`/`speedKey` keys in the `__P1850` literal (core/06-debug.js) — the earlier definitions are silently dead and a future-edit landmine.
9. **D7+S3** `shipPathPoint()` + its wrong "3 call sites" comment (ships.js:410).
10. **S5** the CHUNK-12 "streets note" tombstone naming `paintStub()`/`STREET_MAIN_W/CROSS_W`, none of which exist (ground-paint.js:1532-1545, 14 lines).

---

## D. DEAD CODE (declared, zero call sites — verified by whole-app reference count incl. strings)

**D1. `src/core/05-routing.js:118` — `function graphPath(fromIdx, toIdx)`** (~23 lines incl. its doc comment at 115-117).
Unweighted Dijkstra, superseded by `graphPathWeighted()` (s59 movement naturalism). Zero call sites anywhere
(src, shell, index.html — the only other mentions are its own doc comment and a passing name in the §2 routing
doc block). Its comment even claims it "only ever runs a few hundred times total, all of it once at load while
assigning homes/workplaces" — it never runs at all. **SAFE. ~23 lines.**

**D2. `src/layers/people.js:127-135` — `TRADES`, `TRADES_WITH_CONSTABLE`, `SFPD_FOUNDING_DAY`, `TRADE_EARLIEST_DAY`.**
The pre-catalog ad-hoc trade pool. `TRADES_WITH_CONSTABLE`: 0 refs. `TRADE_EARLIEST_DAY`: referenced only from
the comment at line 132 announcing it. `SFPD_FOUNDING_DAY`: feeds only `TRADE_EARLIEST_DAY`. `TRADES`: feeds only
`TRADES_WITH_CONSTABLE` (03-sim.js:187's "falling back to the legacy ad-hoc TRADES list" console.warn is a lie —
there is no fallback path that reads it). Occupations are fully catalog-driven (`CATALOG_OCC`). **SAFE. ~12 lines**
(also fix the 03-sim warn text — see S-list).

**D3. `src/layers/people.js:378-396` — `function worksiteForTrade(trade, rng)`** (~19 lines).
Legacy trade→worksite switch (incl. the constable station-house case). Superseded by
`worksiteForOccupation()`/`VENUE_KEY_RESOLVERS` (catalog venueNeeds). Zero call sites. **SAFE. ~19 lines.**

**D4. `src/layers/people.js:234-238` — `function loopPoints(cx,cz,r,n)`** (~5 lines).
Old circular-wander generator; extras now use `buildWanderPoly()` (core/05-routing) or the Mission-road polyline.
Zero call sites. **SAFE. ~5 lines.**

**D5. `src/layers/buildings.js:1849` — `var WORKSITE_CHURCH`.**
Chapel worksite object; nothing resolves to it (VENUE_KEY_RESOLVERS has no chapel key; church attendance routes
via `CHURCH_SPOT`/`churchPoly`). Zero refs. **SAFE. 1 line.**

**D6. `src/layers/ground-paint.js:940-944 — `STREET_DIRT_COL`, `SURVEY_GHOST_COL`** + the 3-line
"STREET-CHECKPOINT RESET (2026-07-10)" comment attached to them. Zero refs — per-class colors now come from
`ROAD_COLS`/`ROAD_LAYER_ALPHA` in the s62 constant-width painter. **SAFE. ~5 lines.**

**D7. `src/layers/ships.js:410` — `function shipPathPoint(v,t,arriving)`.**
Compat wrapper around `shipSailPos()` whose comment says "legacy name, 3 call sites" — actual call sites today: **0**
(all callers were migrated to `shipSailPos`, incl. core/06-debug's `sailAudit`). **SAFE. 1 line.**

**D8. `src/layers/fauna.js:568` — `var _faunaM4`.** Unused scratch Matrix4 (fauna placement goes through
`setPersonInstance`/`setShipInstance` scratch objects instead). **SAFE. 1 line.**

**D9. `src/layers/people.js:1152` — `LIMB_STATIC`** (first member of the limb-id enum line). Never referenced —
the shader tests numeric ranges and geometry builders default `l:0` implicitly via `p.l||0`. The rest of the enum
line is live. **SAFE** (drop just the name, or leave as self-documentation — 0 net lines).

**D10. `src/core/06-debug.js:26 and :45` — duplicate keys in the `__P1850` object literal.**
`watch: function(on){…}` (line 26) is silently overridden by `watch: setWatchMode` (line 141); the
`get speedKey()` at line 45 is overridden by the identical getter at line 142. The earlier members are dead and
any future edit to them would be a no-op trap. **SAFE — delete one of each pair. ~4 lines.**

**D11. `src/core/01-geography.js:61-70` — `VARA_M`, `LOT_M`, `BLOCK_W`, `BLOCK_D`.**
`BLOCK_W`/`BLOCK_D` are referenced only from a comment (line 339 explains the plaza now reads street centerlines
instead). `LOT_M` feeds only them; `VARA_M` feeds only `LOT_M`. The whole chain is dead code carrying research
metrology. **SAFE to delete; consider preserving the vara/block figures as a comment** since they cite
geography-shoreline.md §2. ~10 lines.

**D12. `src/core/03-sim.js:196` — `CATALOG_ACT_BY_ID`** built and populated, never read (activities are consumed
by iterating `CATALOG_ACT`). **SAFE. ~2 lines.**

**D13. `src/core/03-sim.js:198-200` + `src/shell.html:829` + `app/catalog-reactions.js` — the reactions catalog.**
`CATALOG_REACT`/`CATALOG_REACT_BY_TYPE` are loaded, indexed, warned-about… and consumed by nothing. The comment
says "wired into the ticker/reaction layer in a later pass." **RISKY — planned feature.** If it's still planned,
keep; if the plan is dead, this deletes ~4 src lines, a script tag, and an entire sidecar download.

---

## M. MISPLACED CODE (wrong layer per layers-spec.md OWNS/NEVER)

**M1. `src/layers/fauna.js:811-846` — people/sim home-assignment machinery in the fauna layer.** *(biggest one)*
`HOME_BUILDING_SPOTS`, `homeBuildingPtr/homeTentPtr`, `nextHome()`, `homeLabelFor()` (people-layer concerns —
people.js calls them to home every routine slot) **and** `dayForDensityAtLeast()` (a DENSITY_CURVE inverter that
core/sim owns conceptually and that **buildings.js** `dayForTentIndex`/`dayForBuildingIndex` — the P0-1 time-jump
fix — depends on). Fauna is now load-bearing for the growth system. Move `dayForDensityAtLeast` → core/03-sim,
the rest → people.js. ~36 lines. **SAFE code motion (chunk renumber + rebuild required).**

**M2. `src/layers/terrain.js:449-686` — fauna + vegetation inside terrain (`buildWestGeography`).**
Sea-lion, duck, and deer instancing/behavior (spec lists *sea lions* by name under fauna's OWNS) and the tule-marsh
vegetation scatter (doodads OWNS vegetation instancing) live in terrain.js. The file header quietly widened its own
charter to "west-peninsula geography" — the spec's terrain OWNS line has no such clause. ~200 lines (fauna ~150,
flora ~30). **RISKY-to-move only in effort terms** (registered `tagInspect` species + `window._westFauna` QA hook
must move intact); behavior-safe.

**M3. `src/layers/terrain.js:673-733` — geographic label DOM + per-frame updater in terrain.**
`LABELS.push(…)` (11 west-peninsula entries), `buildLabels()` (creates `.wlbl` DOM, appends to #hud),
`_geoLabelBandOpacity()`, `updateLabels()`. labels-inspect OWNS "all floating labels + declutter" (the pool array
`geoLabelEls` and `LABELS` itself are declared in labels-inspect.js:935/969). Terrain writing another layer's
globals violates the shared-interface rule. ~60 lines → labels-inspect. **SAFE code motion.**

**M4. `src/layers/ground-paint.js:386-390` — `terrainMat` + `terrainMesh` built and scene-added in ground-paint.**
Terrain OWNS "terrain meshes/materials". It sits here only because `patchGroundDetailMaterial` lives here; the
patch function is the legal shared piece, the mesh/material construction is not. 5 lines → terrain.js (or declare
the seam in the spec). **SAFE motion, but it's the terrain mesh — verify pixel-identical after.**

**M5. `src/layers/ground-paint.js:1605-1640` — route-label pushes into `LABELS`** ("ROAD TO THE MISSION",
"EL CAMINO REAL", "ROAD TO THE PRESIDIO") — same cross-layer global write as M3. ~6 lines.

**M6. `src/layers/buildings.js:596-620` — `makeOutpostPersonGeo()` / `placeOutpostPerson()`.**
The coat-cone-era person geometry (cylinder coat + sphere head) survives here and is consumed cross-layer by
people.js (leveling-crew workers :70, bundle-passenger geo :627) and buildings' own construction workers. People
OWNS person geometry; people.js:1104 declares "the coat-cone era is over" while three systems still render it.
~25 lines → people.js (or an explicit spec-blessed "static-figure prefab" interface). **SAFE motion.**

**M7. `src/layers/buildings.js:1850-1876` — `VENUE_KEY_RESOLVERS` + `worksiteForOccupation()`.**
Schedule/venue binding for people, implemented by calling people.js internals (`pickShopSite`, `pickGamblingSite`,
`pickConstructionSite`, `pickAnyShopSite`) plus ships/doodads worksites — a two-way layer tangle. People OWNS
"schedules/routing consumption". ~27 lines → people.js. **SAFE motion.**

**M8. `src/layers/effects.js:802-833` — `resetFleeBudget()` / `applyFireCrowd()`.**
Writes `slot._x/_z/_pose/_now` — it *moves people* (fire flee/spectate), called from people.js per slot per frame.
The documented Great-Split carry-over note (layers-spec §Assembly) covers only the fire **structure-damage vertex
work**; this second interleave is undocumented. Either move to people.js or add it to the declared seam list.
~32 lines. **SAFE motion.**

**M9. `src/core/03-sim.js:74-114` — speed-pill DOM wiring + global keydown (Space/1/2/3/4) in core.**
Spec: core/sim "No rendering"; camera-input OWNS "pointer/touch/keys, speed pill + menu chrome bindings".
`updateSpeedPill`, the pill click listener, and the keyboard listener belong in camera-input; `setSimSpeed`
state stays core. ~40 lines. **SAFE motion.**

**M10. `src/layers/ui-chrome.js:174-193` — `flyTo()` (+ its `pulseAt` crosshair).**
Writes `CAM.focusT`/zoom — the camera rig is camera-input's. Consumed by labels-inspect, director, debug, ui-chrome.
Either move to camera-input or bless `flyTo` as a shared interface in the spec (it behaves like one). ~20 lines.

**M11. `src/layers/zones-tint.js:157,183-188` — cross-layer mesh/material writes.**
Gates `RANCHERIA_MESH` (a buildings-layer mesh) visibility and writes `buildingsMesh`/`growth*Mesh`/`tentMesh`
`.material.opacity` for the semantic-zoom crossfade. Buildings should expose a `setStructureFade(t)` interface.
**Side-finding (latent bug): `growthBuildingMesh2` is missing from the opacity list — colorway-2 growth buildings
never fade at carto altitude.** ~8 lines.

**M12. `src/layers/labels-inspect.js:608` — `NAMED_BUILDING_SPOTS`.**
Derived buildings data declared in labels-inspect but consumed by buildings.js (landmark signage :1730) and
people.js (`CITY_HOTEL_SPOT` :410). Belongs beside `VILLAGE_BUILDING_SPOTS` in buildings.js. 1 line.

**M13. `src/layers/terrain.js:444-447` — `OUTPOSTS` + `src/layers/doodads.js:955-978` — `LUMBER_YARD_SPOT`.**
`OUTPOSTS` is geography truth read by **core**/02-scene (`PLACEMENT_MISSION_SEGS`) — a core→layer dependency
inversion; belongs in core/01-geography. The lumber yard is a signed building + worksite built inside doodads
(buildings OWNS structures/signs). ~30 lines combined.

---

## U. DUPLICATION

**U1. Point-to-segment distance — five implementations.**
`distToSegXZ` (core/02-scene.js:193, the canonical), `_distPtSeg2` squared-variant (core/04-roads.js:126),
`segProj` inline (core/05-routing.js:473), `_ptSegDistSq` (ships.js:345), `_distToSegXZ` (labels-inspect.js:1176),
`distToSeg` local (doodads.js:1095). The squared variants are hot-path-justified; the plain re-implementations in
labels-inspect and doodads are not — both can call the core helper. **SAFE consolidation, ~15 lines.**

**U2. `pick(arr,h)` defined twice inside doodads.js** (lines 297 and 558, byte-identical). Hoist one. ~2 lines.

**U3. Catmull-Rom implemented twice** — `_catmull` (terrain.js:28) and `cr` inside `smoothShipPath`
(ships.js:264-268). Could share a core helper; low value, both are local and stable. **Optional.**

**U4. `escapeHTML` / `cap` live in ui-chrome.js (:94/:132) but their heaviest consumer is labels-inspect
(38 `escapeHTML` calls) plus core/05-routing (`cap`).** Cross-layer internal calls; both are exactly the kind of
helper core/00-boot owns. **SAFE motion, 2 lines.**

**U5. `DOW_NAMES` declared in people.js:443, consumed by doodads.js:686 (`isWashDay`).** Move to core (calendar
is core's clock domain). 1 line.

**U6. `_microHash` / `_doodadHash`** (doodads.js:168/410) — same one-line hash2 wrapper pattern with different
constants; fine functionally, worth a shared `cellHash(ix,iz,k,c1,c2…)` only if touched anyway. **Optional.**

**U7. Tiny radial-gradient canvas sprites built ~6 ways** — ground-paint `blotchSprite`, effects smoke/fog/contact-
shadow/puddle textures, people `LANTERN_POOL_TEX`. A shared `radialSpriteTex(stops,size)` would drop ~25 lines.
**Optional, SAFE.**

---

## S. STALE COMMENTS / DOCS (the "Frankenstein callback" fuel)

| # | Where | What it wrongly claims | Fix |
|---|---|---|---|
| S1 | `core/07-main.js:52` | "the existing **spring-damper camera rig** does the rest, same as flyTo()" | Rig is the Annals chase rig (camera-input.js:12 explicitly says spring-damper is REPLACED). Reword to "the chase rig". |
| S2 | `director.js:13` | "shot grammar = eased fly-to (the existing **spring rig**)" | Same dead system. |
| S3 | `ships.js:410` | "legacy name, **3 call sites**" | Zero call sites — the wrapper itself is dead (D7). |
| S4 | `camera-input.js:153` | "label chips (**.ship-label/.building-label**, enlarged to finger size…)" | Those classes were deleted in the haloed-label rework; today's classes are `.wlbl*`/`.street-label`/`.biz-glyph`. |
| S5 | `ground-paint.js:1532-1545` (CHUNK 12 "streets note") | Points readers at "`paintStub()` up near the terrain build" and "`STREET_MAIN_W/CROSS_W`" as living single-sources-of-truth | Neither identifier exists anywhere. Rewrite the tombstone or delete the chunk body (keep the chunk marker for build order). |
| S6 | `zones-tint.js:13` | "name chips (the existing **.geo-label** look)" | `.geo-label` CSS/class is gone; chips were replaced (its own line 145 says so). |
| S7 | `people.js:130-132` | "'constable' only enters the trade pool once… see TRADE_EARLIEST_DAY below, folded into each routine slot's notBeforeDay" | Not folded into anything — the whole mechanism is dead (D2). Occupation gating rides the catalog now. |
| S8 | `core/05-routing.js:115-117` | graphPath "runs a few hundred times total… assigning homes/workplaces" | Never runs (D1). |
| S9 | `core/01-geography.js:63-67` | "Only coarse consumers (plaza fallback math) read this [BLOCK_W]" | Nothing reads it (D11); plaza math reads centerlines (line 339). |
| S10 | `core/03-sim.js:187` | warn text "falling back to the legacy ad-hoc TRADES list" | No such fallback exists; if the catalog fails to load, occupations are simply null. |

(Deliberately **not** flagged: the many "X was DELETED, here's why" history notes — e.g. drapeGroundStrip at
02-scene.js:139, DAY_CYCLE_SECONDS at effects.js:838, A1-ripple at terrain.js:224/doodads.js:12, width-taper at
ground-paint.js:594 — those correctly describe systems as dead and are protective documentation.)

---

## C. CSS ORPHANS (`src/shell.html`)

Automated pass: 173 rules / 110 unique class+id tokens parsed; every token cross-checked against the HTML body and
all src JS (className/classList/querySelector/innerHTML strings).

**C1. `.pp-nopaper-since` (shell.html ~line 306)** — zero references anywhere in body or JS (the paper pane's
no-issue state renders `.pp-noissue` only). **SAFE. ~4 lines.**

Everything else — including all `.wlbl-*` variants, `.fi-*`, `.hm-*`, `.sp-seg`, `.scr-*` — is referenced. The
stylesheet is remarkably clean.

---

## X. DATA-SIDECAR SPOT-CHECK (fields consumed by nothing in src)

**X1. `catalog-reactions.js` — the entire sidecar** is loaded and indexed but consumed by nothing (see D13).

**X2. `events-1846-49.js` — the `pos` field** (a location string on all 2,628 records) is never read; place
resolution goes through headline/text regex (`resolvePlace`, ui-chrome). Also `actors` is read only by the
director's person-beat scan — fine. `pos` is builder-side bloat only (tools/build-events.js could drop it).

**X3. `prices-1846-49.js` — `kind` and `ctx` fields** (3,705 records) never read in src; consumers use
item/price/priceHigh/unit/date/src/url. Builder-side trim candidate only.

Roster fields (name/role/occ/gender/first/last/mentions/src): **all consumed.** Feed fields
(type/headline/text/prov.src/prov.quote, masthead): **all consumed** (`prov.quote` via receipt hover? —
`quote` is not read in src; borderline, left off the list since receipts are built from `prov.src`).

---

## Notes for the fixing sprint (not defects, just handling)

- Every "move" item requires the chunk-order discipline: new `@P1850-CHUNK` homes must keep global statement
  order byte-stable (tools/build-app.js `--check` + the six-framing pixel compare per layers-spec §Assembly).
- The documented effects↔buildings fire interleave stays as-is per the spec; M8 (fire crowd) should be either
  moved or *added to the documented seam list* in the same commit — right now it's an undocumented second seam.
- If M1/M7 land, buildings.js and fauna.js each lose their last people-domain tendrils, which makes future
  "diff may touch nothing else" boundary checks (QA-GATE) actually enforceable for those layers.
